# public-web

前台访客端骨架（React + Vite + TypeScript），当前仅一个空白欢迎页，
不含 F-002 的具体页面逻辑。按 ADR-028 与后台分开部署、不同域。

## 启动

```bash
npm install
npm run dev       # http://localhost:5173
npm run build      # 产出 dist/
```
