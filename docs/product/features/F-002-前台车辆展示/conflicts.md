---
feature: F-002
generated_from: review/*.md（含 2026-08-19 初轮 reviewer-*.md 与 2026-08-20 复核轮 *.md 共 5×2 份）
---

> 说明：review/ 下存在两轮文件——`reviewer-{role}.md`（2026-08-19，针对早期草稿）与 `{role}.md`（2026-08-20，针对 spec v1.4 复核）。domain/admin 复核轮明确指出多数早期歧义已在 spec v1.4 中定稿收敛；本表如实保留两轮原始主张，供交叉核对。

## 一致项

- 前台是哑终端：只渲染 `/public/*` 已返回数据，不做状态过滤、不做价格类型（面谈/数字）业务判断（api/domain/qa/admin/miniapp 各轮一致）。
- 可见性唯一真相是服务端 `published` 过滤；非 published 与不存在统一按契约失败态处理，禁止 403（api/domain/qa 一致）。
- 公开响应禁止出现 VIN、操作人、内部备注、version（api/domain/qa/admin 一致）。
- 当前价必须等于最新一条价格记录的变动后值，前台不得自行计算/拼装（api/domain/qa 一致）。
- 对象存储图片/评估报告直链下架后不失效，属已知限制（ADR-009/010），各轮均确认接受、非本轮新风险。
- 本 feature 不新增任何写入接口，不改车辆/图片/价格的写入路径（除管理后台新增"复制前台链接"这一纯前端只读辅助）（api/domain/admin 一致）。
- 评估报告新标签打开原文件直链，不做内嵌预览/转码（api/domain/qa/miniapp 一致）。
- 明确不做微信小程序、不做微信专属能力（JS-SDK、openid、自定义分享卡片）（miniapp 两轮、admin、qa 一致）。
- 图片按公开接口返回顺序展示，顺序即展示真相，前台不得重排（api/domain/qa 一致）。
- 手机竖屏优先、桌面兼容，但桌面无强验收口径（各轮一致，qa 标记为低优先级）。

## 冲突项

### C-1 面谈价格的展示文案应由谁给出
- 各方主张：api（08-19）主张倾向服务端下发展示字符串，若写死在前端则前端做了业务判断，与哑终端原则冲突 ／ admin（08-19）主张文案可在前台写死，只要前后台用词统一定稿即可，不强制服务端下发结构化文案。
- 分歧根因：对"哑终端"边界理解不同——纯展示的死映射（type→文案）算不算业务判断。
- 选项 A：服务端价格 DTO 新增展示字段（如 display 字符串），前台零分支 — 代价：需 RFC 改 F-004 公开契约。
- 选项 B：前台维护唯一定稿的 type→文案死映射表，后台用词与之对齐 — 代价：文案变更需前后端联动改，且需流程约束防止两端漂移。
- 备注：domain（08-20）称"面议"作为价格类型取值、金额置空的死态处理已在 spec v1.4 收敛，此条可能已被裁决，但两轮的具体落地方式（A/B）在 review 原文中未见明确记录，建议核对 spec 定稿条款。

## 无人认领的空白

- **微信域名 ICP 备案与微信安全中心检测**：miniapp（08-20）指出若主站或 OSS 域名未备案/被判定异常，微信会整页拦截访客，"这不是代码能解决的问题，需要业务方在上线前确认"——但没有任何角色（含 admin/运维）明确认领"谁去核实备案状态、谁去做安全中心申诉"。
- **PUBLIC_WEB_ORIGIN 环境变量的实际配置**：admin（08-20）指出管理后台需要该变量才能拼出正确的前台分享链接，需"运维侧确认"，但没有角色认领具体由谁在什么时间点提供/校验该配置项。
- **图片/报告与车辆 ID 的归属校验落地方**：qa（reviewer-qa 08-19 及 qa 08-20）反复强调"图车错配、报告串号是零容忍项，前台测试发现错配应先归后台/服务端数据，不是前台做校验逻辑"，但 api/domain 两轮均未明确表态"由服务端实现归属校验测试/断言"，责任在服务端与 QA 之间悬空。
- **CDN/浏览器缓存导致"下架立即不可见"失效的兜底**：api、domain、qa 三方各自提到公开 GET 缓存策略、TTL 需要定，但都把决定权推给"契约/运维"，review 中无人明确认领"由谁设置并验证该 TTL"。
- **评估报告在微信 WebView 内三级降级链的真机验证**：miniapp（08-20）指出该降级链"仅为设计预期，尚未验证"，qa 的测试矩阵（reviewer-qa 08-19）只写"微信内置浏览器手动抽检"，未点名必须覆盖"点击评估报告"这一具体交互，无人认领谁去做这项真机测试。
- **详情页并发请求多资源（图片/报告/价格）中某一项 404 时的整页失败态**：reviewer-qa（08-19）与 api（08-19，隐含要求段）都提到"禁止用部分成功数据拼出错误印象"，但未见任何角色说明由谁设计/实现这个统一失败态的具体交互方案。
- **运营发布前"以访客身份预览"的能力缺口**：reviewer-admin（08-19）指出已上架编辑立即生效、运营没有预览稿，容易把已上架详情当草稿箱改，属于价格/图片错误零容忍的高风险来源，但 admin（08-20）复核轮未再提及此问题是否已被处理或有意不做，没有角色认领后续跟进。

## 需要用户裁决的问题

1. 「当前价」的实现方式：车辆表冗余只读字段（写价格记录时同步更新）还是查询时实时从价格记录表取最新一条？（api）
2. IP 级限流是否按单实例进程内存计数起步，未来水平扩展时再引入集中式存储（如 Redis）？（api）
3. 列表页展示条数与是否分页，具体规则以 brief 还是 spec 为准（qa 指出二者存在不一致风险）？（qa）
4. 价格历史为空（新车尚无变动记录）时前台展示什么文案，是否属于本期验收范围？（qa）
5. 已下架/回收站车辆"不可见"的验收判定标准：详情 404、列表消失、还是二者都要求，以哪个作为验收口径？（qa，reviewer-qa/reviewer-admin/reviewer-domain 08-19 均提出同一问题）
6. 图片说明为空、评估报告缺失、图片数量为 0（边界防御场景）时前台是否原样渲染、是否允许空白区块？（qa）
7. 主站与 OSS（图片/评估报告）域名是否已完成 ICP 备案，是否需要在微信安全中心做域名检测/申诉以避免被拦截？（miniapp）
8. 评估报告"新标签→当前 WebView→提示手动打开"三级降级链是否已在微信 iOS/Android 真机上验证过 PDF 与图片两种文件类型？（miniapp）
9. 管理后台构建/运行环境是否已有前台生产域名可用的环境变量（`PUBLIC_WEB_ORIGIN`），还是需要新增一个部署配置项？（admin）
10. 面谈价格的展示文案最终由服务端下发结构化字段，还是由前台维护唯一死映射表（对应冲突项 C-1，若 spec v1.4 已定稿请核对具体落地方式是否与此一致）？（api / admin 分歧）
